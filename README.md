# Personal Web

A static personal website scaffold built with Next.js App Router and React. The current milestone focuses on a responsive layout with a shared header and footer.

## Scripts

- `npm run dev` — start the local development server.
- `npm run build` — create a static production export in `out/`.
- `npm run lint` — run ESLint.
